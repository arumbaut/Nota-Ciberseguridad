Este filtro marca cualquier peticion arp que provenga de la puerta de enlace que no tenga la direccion mac de la puerta de enlace

```
((arp.src.proto_ipv4 == ip_gateway) && (arp.opcode==2)) && !(arp.src.hw_mac==mac_gateway)
```

For your reference, the filter that I show you how to build in the video is this one:

```
((arp.src.proto_ipv4 == 10.0.0.1) && (arp.opcode == 2)) && !(arp.src.hw_mac == 11:22:33:44:55:66)
```