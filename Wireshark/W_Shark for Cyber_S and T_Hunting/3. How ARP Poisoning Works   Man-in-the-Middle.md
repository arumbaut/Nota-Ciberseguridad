Comando para que nuestro sistema reenvie las peticiones de cada maquina de lo contrario no podremos reenviar las peticiones al gateway y biseversa

```
sysctl net.ipv4.ip_forward=1
```

En ettercap la opcion Bridged sniffing es para cuando tenemos 2 interfaces conectadas al ruter para que las peticiones entren por una y salgan por otra.

1- Debemos especificar a Ettercap cuales son los Targets

Target1 - Maquina Objetivo 
Target2 - Gateway de la red.

Wireshark-> Address Resolution Protocol (ARP) -> Opcode:reply(2) 
Opcode -> Respuesta de codigo de Operacion
Lo normal es Opcode(1)
\#Esto se reconoce como una respuesta ARP no solicitada.