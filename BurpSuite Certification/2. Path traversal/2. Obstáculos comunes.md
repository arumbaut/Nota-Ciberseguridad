## Obstáculos comunes para explotar vulnerabilidades de cruce de caminos

Muchas aplicaciones que colocan la entrada del usuario en rutas de archivos implementan defensas contra ataques de cruce de rutas. A menudo es posible pasarlos por alto.

Si una aplicación elimina o bloquea secuencias de recorrido de directorio del nombre de archivo proporcionado por el usuario, podría ser posible eludir la defensa utilizando una variedad de técnicas.

Es posible que pueda utilizar una ruta absoluta desde la raíz del sistema de archivos, como `filename=/etc/passwd`, para hacer referencia directa a un archivo sin utilizar ninguna secuencia transversal.

## Obstáculos comunes para explotar vulnerabilidades de cruce de caminos - Continuación

Es posible que puedas utilizar secuencias transversales anidadas, como `....//` o `....\/`. Estos vuelven a secuencias transversales simples cuando se elimina la secuencia interna.

Ejemplo peticion 
```
#Comun
GET /image?filename=../../../etc/passwd

GET /image?filename=....//....//....//etc/passwd 
```


## Obstáculos comunes para explotar vulnerabilidades de cruce de caminos - Continuación

En algunos contextos, como en una ruta URL o la `filename` parámetro de a `multipart/form-data` En caso de solicitud, los servidores web pueden eliminar cualquier secuencia de recorrido de directorio antes de pasar su entrada a la aplicación. A veces puedes evitar este tipo de desinfección mediante codificación de URL, o incluso codificación de URL doble `../` personajes. Esto da como resultado `%2e%2e%2f` y `%252e%252e%252f` respectivamente. Varias codificaciones no estándar, como `..%c0%af` o `..%ef%bc%8f`, también puede funcionar.

Para los usuarios de Burp Suite Professional, Burp Intruder proporciona la lista de carga útil predefinida **Fuzzing - recorrido de ruta**. Esto contiene algunas secuencias de recorrido de ruta codificadas que puedes probar.

Podemos encodear utilizando el Decoder de burpsuit y encodear todas las veces que deseemos ejemplo

```
/ Valor despues de encodear en 2 ocaciones
..%25%32%66..%25%32%66..%25%32%66etc/passwd
```

```
Otras encodificaciones
../../../ => %25%32%65%25%32%65%25%32%66%25%32%65%25%32%65%25%32%66%25%32%65%25%32%65%25%32%66

/ => %25%32% 
%25%32%65%25%32%65%25%32%66%25%32%65%25%32%65%25%32%66%25%32%65%25%32%65%25%32%66etc%25%32%66passwd 
```