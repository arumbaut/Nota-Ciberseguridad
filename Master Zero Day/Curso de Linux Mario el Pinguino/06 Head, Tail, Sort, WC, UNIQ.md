```
head    #muestra el top del documento

tail    #muestra la parte final

sort    #para ordenar la salida
sort -n file  #ordena de menor a mayor
sort -n -r file  #ordena de mayor a menor
sort -n -u file  #elimina duplicados

wc      #cuenta las lineas de salida
wc -l   #cuenta las lineas
wc -w   #cuenta palabras
wc -c   #cuenta caracteres


uniq  #elimina duplicados en la salida
```