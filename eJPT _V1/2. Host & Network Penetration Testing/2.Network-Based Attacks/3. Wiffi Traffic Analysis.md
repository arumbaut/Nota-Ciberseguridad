 Protocolo 802.11

Filtros
```
(wlan.fc.type_subtype == 0x0008) && !(wlan.wfa.ie.wpa.version == 1) && !(wlan.tag.number == 48)

```

Este es un **filtro de visualización de Wireshark**, probablemente usado para detectar **beacon frames que no usan WPA o RSN (WPA2)**, es decir, redes inalámbricas abiertas o sin cifrado fuerte.

## 🧪 En Wireshark

Puedes capturar beacon frames con una tarjeta Wi-Fi en modo monitor.  

Filtro útil:

```
wlan.fc.type_subtype == 0x08
```

O simplemente:

wireshark

```
wlan.fc.type_subtype == `
```


Buscar cualquier paquete Wi-Fi que contenga la cadena de texto "LazyArtist" en su contenido de capa WLAN.
```
wlan contains LazyArtist
```

## Filtro más específico (si es un SSID):

```
wlan.ssid == "LazyArtist"
```

Este es un **filtro de visualización** que busca **beacon frames** cuya **SSID contenga la palabra "Amazon"**.

```
(wlan.ssid contains Amazon) && (wlan.fc.type_subtype == 0x0008)
```


Este filtro muestra únicamente los paquetes que cumplen con estas dos condiciones:

1. `wlan.bssid == e8:de:27:16:87:18`  
    → Filtra paquetes cuyo **BSSID (MAC del punto de acceso)** sea `e8:de:27:16:87:18`.
    
2. `wlan.fc.type_subtype == 0x0020`  
    → Filtra paquetes que sean del tipo **Data subtype: QOS Data (0x20)**.
```
((wlan.bssid == e8:de:27:16:87:18) && (wlan.fc.type_subtype == 0x0020))
```
