whatweb ip 

http ip 

Para hacer directory traberce al igual que gobuster
dirb http://ip

Enumeracion web
browsh --startup-url http://ip

Nmap scrips

```
nmap ip -sV -p 80 --script http-enum

nmap ip -sV -p 80 --script http-headers

nmap ip -sV -p 80 --script http-methods --script-arguments http-methods.url-path=/webday/

nmap ip -sV -p 80 --script http-webdav-scan --script-arguments http-methods.url-path=/webday/
```

Para apache
```
nmap ip -sV -p 80 -script banner
```

Metasploit
```
mfsconsole
>use auxiliary/scanner/http_version
>set rhost ip
>options
>run
```

```
mfsconsole
>use auxiliary/scanner/http/robots_txt
>set rhost ip
>options
>run
``` 

Para directory traverse
```
mfsconsole
>use auxiliary/scanner/http/brute_dirs
>set rhost ip
>options
>run
```

```
dirb http://ip /usr/share/metasploit-framework/data/wordlists/directory.txt
```

```
curl ip | more

wget "http://ip/index"
```

Renderiza en la linea de comandos
```
browsh --starup-url ip
```

```
lynx "http://ip/"
```


```
curl http:ip/cgi-bin/ | more
```