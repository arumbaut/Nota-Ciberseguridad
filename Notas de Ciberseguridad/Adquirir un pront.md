- Para iniciar una nueva sesión de Bash de forma limpia, con el entorno reconfigurado por `script`.
    
- En algunos casos, se usa para forzar la creación de un pseudo-terminal (pty), por ejemplo:

script /dev/null -c bash

