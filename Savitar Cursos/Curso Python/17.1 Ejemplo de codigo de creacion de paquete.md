
 Creamos una carpeta hack4you que sera el paquete , luego el archivo `__init__.py` , couses.py y utils.py  en el archivo init importaremos los modulos para hacer las importaciondes de pyton de manera simple en la misma carpeta donde esta hack4you creamos los archivos setup.py y README.md

Archivo `__init__.py`  en el directorio hack4u 

```
from .courses import *
from .utiles.utils import *
```


```
class Course:
    def __init__(self, name, duration, link):
        self.name = name
        self.duration = duration
        self.link = link

    def __repr__(self):
        return f"{self.name} [{self.duration} horas] ({self.link})"

courses = [
    Course("Introducción a Linux", 15, "https://hack4u.io/cursos/introduccion-a-linux/"),
    Course("Personalización de Linux", 3, "https://hack4u.io/cursos/personalizacion-de-entorno-en-linux/"),
    Course("Introducción al Hacking", 53, "https://hack4u.io/cursos/introduccion-al-hacking/")
]

def list_courses():
    for course in courses:
        print(course)

def search_course_by_name(name):
    for course in courses:
        if course.name == name:
            return course
    return None
```

Archivo `__init__.py`  en el directorio utiles  si lo dejas en blanco solo lo necesitas importar en el init base en este caso en el del archivo hac4u/`__init__.py`

```
from .utils import *
```

Utiles en un directorio independiente
```
from ..courses import courses

def total_duration():

    return sum(course.duration for course in courses)
```



Para subir más de un nivel en la jerarquía de directorios de Python, simplemente añades más puntos al inicio de la ruta de importación.

Cada punto adicional representa subir un nivel exacto en la estructura de carpetas:

- **`.` (Un punto):** Directorio **actual**.
    
- **`..` (Dos puntos):** Directorio **padre** (sube 1 nivel).
    
- **`...` (Tres puntos):** Directorio **abuelo** (sube 2 niveles).
    
- **`....` (Cuatro puntos):** Directorio **bisabuelo** (sube 3 niveles).
    

---

### Ejemplo práctico de estructura

Si tuvieses un proyecto más profundo como este:

Plaintext

```
proyecto/
├── app/
│   ├── main.py
│   └── data/
│       └── models.py
└── utils/
    └── helpers/
        └── formatter.py  <-- Estamos aquí
```

Si estás editando `formatter.py` y necesitas importar algo de `models.py`, tendrías que subir **dos niveles** (de `helpers/` a `utils/`, y de `utils/` a `proyecto/`) y luego bajar a `app/data/`

```
# Dentro de formatter.py from ...app.data.models import MiClase
```

Setup.py
```
from setuptools import setup, find_packages

# Leer el contenido del archivo README.md
with open("README.md", "r", encoding="utf-8") as fh:
    long_description = fh.read()

setup(
    name="hack4u",
    version="0.1.4",
    packages=find_packages(),
    install_requires=[],
    author="Marcelo Vázquez",
    description="Una biblioteca para consultar cursos de hack4u.",
    long_description=long_description,
    long_description_content_type="text/markdown",
    url="https://hack4u.io",
)
```