
Sin este comando configurado en el router. El router te permitirá configurar una dirección IPv6 incluso hacer ping a las direcciones IPV6 en sus interfaces lo que no permitirá es hacer forward del trafico IPv6

```cisco
RouterA(config)#ipv6 unicast-routing

```