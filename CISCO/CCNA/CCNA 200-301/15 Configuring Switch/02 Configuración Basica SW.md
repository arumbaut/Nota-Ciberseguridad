- Tags : #switch_basic_conf

#### Configuraciones Básicas
```cisco
switch>enable
switch#configure terminal
switch(config)#hostname Sw1
switch(config)#ip domain-name estudio.local
switch(config)#banner motd #Este es el sw de PEPE mantente alejado#
switch(config)#
```

